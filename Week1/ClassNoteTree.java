package Week1;

public class ClassNoteTree {
	public void Height() {
		//	the input argument should include tree
		//	if tree== nil: return 0
		//	return 1+max(Height(tree.left), Height(tree.right))
	}
	public void size() {
		// the input arg should include tree
		// if tree == nil
		//		return 0
		// 	return 1 + size(tree.left) + size(tree.right)
	}
}
